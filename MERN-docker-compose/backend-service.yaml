apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend
  name: backend
spec:
  type: LoadBalancer
  ports:
    - name: "backend-port"
      protocol: TCP
      port: 5050
      targetPort: 5050
  selector:
    app: backend
